@model IEnumerable<Units>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notifications" />
<div class="container p-3">
    <div class="col-6 text-end">
        <a asp-controller="Units" asp-action="Create" class="btn btn-primary">Create new</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Name
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr>
                    <td width="25%">
                        @obj.Id
                    </td>
                    <td width="50%">
                        @obj.Name
                    </td>
                    <td width="25%">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Units" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary">Edit</a>
                           <a asp-controller="Units" asp-action="delete" asp-route-id="@obj.Id" class="btn btn-danger">Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>